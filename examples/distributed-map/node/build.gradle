apply from: rootProject.file("../../gradle/dagger.gradle")

dependencies {
    compile project(':distributed-map-shared')
    compile group: 'io.morethan.tweaky', name: 'tweaky-node', version: tweakyVersion
}