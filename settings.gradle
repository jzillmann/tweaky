rootProject.name = 'tweaky'

module('grpc')
module('conductor')
module('conductor-shared')
module('node')
module('node-shared')
module('test')

def module(String shortName) {
    def name = ":tweaky-$shortName"
    include name
    project(name).projectDir = file("modules/$shortName")
}

